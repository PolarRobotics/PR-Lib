## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: ">=4.1.0"
  # # Put list of dependencies here
  # # For components maintained by Espressif:
  # component: "~1.0.0"
  # # For 3rd party components:
  # username/component: ">=1.0.0,<2.0.0"
  # username2/component2:
  #   version: "~1.0.0"
  #   # For transient dependencies `public` flag can be set.
  #   # `public` flag doesn't have an effect dependencies of the `main` component.
  #   # All dependencies of `main` are public by default.
  #   public: true
  # PR-Lib version of BTStack (required by Bluepad32)
  btstack:
    version: '*'
    git: https://github.com/PolarRobotics/PR-Lib.git
    path: components/bluepad32/external/btstack

  # PR-Lib version of cmd_nvs (required by Bluepad32)
  cmd_nvs:
    version: '*'
    git: https://github.com/PolarRobotics/PR-Lib.git
    path: components/bluepad32/src/components/cmd_nvs

  # PR-Lib version of cmd_system (required by Bluepad32)
  cmd_system:
    version: '*'
    git: https://github.com/PolarRobotics/PR-Lib.git
    path: components/bluepad32/src/components/cmd_system